# Application Configuration
ENVIRONMENT=production
PORT=8080

# =============================================================================
# Cloudflare D1 Database Configuration (Production/Docker)
# =============================================================================
# Required for docker-compose to connect to Cloudflare D1

# Your Cloudflare Account ID
# Find it at: https://dash.cloudflare.com/ (in the URL or sidebar)
CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# Your Cloudflare API Token
# Create at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: D1:Edit
CLOUDFLARE_API_TOKEN=your_api_token_here

# Your D1 Database Name or UUID
# Find it with: npx wrangler d1 list
CLOUDFLARE_DB_NAME=cloudflaredb

# =============================================================================
# Local Development (Non-Docker)
# =============================================================================
# For running the app directly with `go run cmd/api/main.go`
# Uncomment these lines and comment out the D1 config above:

# DATABASE_DRIVER=sqlite3
# DATABASE_DSN=./local.db

# =============================================================================
# How to Get Your Cloudflare Credentials
# =============================================================================
#
# 1. Account ID:
#    - Go to https://dash.cloudflare.com/
#    - The account ID is in the URL or right sidebar
#
# 2. API Token:
#    - Go to https://dash.cloudflare.com/profile/api-tokens
#    - Click "Create Token"
#    - Use "Edit Cloudflare Workers" template
#    - Or create custom token with "D1:Edit" permission
#    - Copy the token (you can only see it once!)
#
# 3. Database Name:
#    - Run: npx wrangler d1 list
#    - Use the database name or UUID from the list
#
# 4. DSN Format:
#    The cfd1 driver uses: d1://account-id:api-token@database-name
#    Docker-compose will construct this automatically from the variables above
